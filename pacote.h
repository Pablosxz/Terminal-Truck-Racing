#pragma once

// Protótipos
unsigned int Empacotar(unsigned int, unsigned int, unsigned int, unsigned int, unsigned int);
unsigned int Passo(unsigned int);
unsigned int Cor(unsigned int);
unsigned int Posicao(unsigned int);
unsigned int Velocidade(unsigned int);
unsigned int Pista(unsigned int);